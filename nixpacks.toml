[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python s2u_project/manage.py collectstatic --noinput"]

[start]
cmd = "gunicorn s2u_project.wsgi:application --bind 0.0.0.0:$PORT"
